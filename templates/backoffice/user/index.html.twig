{% extends 'base.html.twig' %}

{% block body %}
<style>
    .example-wrapper { margin: 1em auto; max-width: 800px; width: 95%; font: 18px/1.5 sans-serif; }
    .example-wrapper code { background: #F5F5F5; padding: 2px 6px; }
</style>

<div class="example-wrapper">
   
      <table class="table">
    <thead>
    <tr>
      <th scope="col">#</th>
      <th scope="col">Email</th>
      {# <th scope="col">Rôle</th> #}
      <th scope="col">Password</th>
      <th scope="col">Nom</th>
      <th scope="col">Prénom</th>
      <th scope="col"></th>
    </tr>
  </thead>
  
  <tbody>
    {% for user in users %}
<tr>
  <td>
      {{ user.id }}
  </td>
      <td>{{ user.email }}</td>
    
      {# <td>{{ user.role }}</td>  #}
    
      <td>{{ user.password }}</td>
    
      <td>{{ user.name }}</td>

      <td>{{ user.lastname }}</td>
   
    <td><a href="{{ path('userModify', {'idUser': user.id}) }}"><button class="btn btn-info" type="button">edit</button></a> <a href="{{ path('deleteUser', {'idUser': user.id}) }}"><button class="btn btn-danger" type="button">delete</button></a></td>
    
  
</tr>
    {% endfor %}
    </tbody>
</table>
    
    <a href="{{ path('userAdd') }}"><button class="btn btn-primary" type="button">Add user</button></a>
</div>
{% endblock %}
